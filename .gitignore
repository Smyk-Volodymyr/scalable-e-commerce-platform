node_modules/
dist/
.env
*.log
# Правило нижче ховає дампи баз (напр. backup.sql), які не мають лежати в репозиторії.
*.sql
# Але міграції — це частина коду сервісу: без них чистий клон і CI не піднімуть схему,
# а Docker-образ отримає порожню теку migrations.
!**/migrations/*.sql
.DS_Store
